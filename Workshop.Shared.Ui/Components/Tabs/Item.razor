@code {

  [Parameter]
  public RenderFragment? ChildContent { get; set; }

  [CascadingParameter]
  public Items? Parent { get; set; }

  protected override void OnInitialized()
  {
    if (Parent != null && ChildContent != null)
    {
      Parent?.AddTab(ChildContent);
    }
  }

}
