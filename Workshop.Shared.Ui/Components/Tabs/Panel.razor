@code {

  [Parameter]
  public RenderFragment? ChildContent { get; set; }

  [CascadingParameter]
  public Panels? Parent { get; set; }

  protected override void OnInitialized()
  {
    if (Parent != null && ChildContent != null)
    {
      Parent.AddPanel(ChildContent);
    }
  }

}
